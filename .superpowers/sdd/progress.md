# M1 Moshi Bring-Up — Progress Ledger

Plan: docs/superpowers/plans/2026-09-02-m1-moshi-bringup.md

Task 1: complete (system install, no commit — CUDA 13.3 installed 2026-09-05, verified via nvcc --version)
Task 2: complete (system install + clone, no commit — Rust 1.98.1 installed, Kyutai repo cloned at ml/moshi/candle-moshi, commit e6a55d2722a65870ef52a6c9f6ecfc0e90f38362)

Discovery during Task 2: real Kyutai protocol (rust/protocol.md) does not match backend/app/services/moshi.py's assumed Tag scheme (Opus vs raw PCM, wss vs ws, tag numbering mismatch). Plan file updated in place to reflect: Task 3 now targets the real `moshi-backend` binary + `config-q8.json` + TLS cert; Task 5 rewritten from a conditional "verify protocol" check into a mandatory Opus/PCM + tag-remap relay (ml/moshi/relay.py). User confirmed this approach (relay, not modifying the backend client) on 2026-09-05.

Task 3: complete (driven directly, not via subagent — hands-on GPU build with 3 real blockers hit and fixed: cudarc's CUDA-13 version-detection panic worked around by installing CUDA 12.8 side-by-side and pointing PATH at it for the build; nvcc needed vcvars64.bat sourced for cl.exe; sentencepiece-sys needed CMAKE_POLICY_VERSION_MINIMUM=3.5 for CMake 4.x. moshi-backend.exe built successfully, q8 weights auto-fetched, server confirmed listening on https://0.0.0.0:8998, HTTP 200 verified. Full details in docs/M1_BRINGUP_LOG.md. Server currently left running in background per user request, so Task 5 verification doesn't need a cold restart.)
Task 4: complete (gitignore entries added for ml/moshi/candle-moshi/, weights-q8/, weights-q4/; verified untracked via git status --short)

Task 5: complete (commits c879605..e73c897, review clean — Approved, no Critical/blocking Important findings). Relay built at ml/moshi/relay.py, listens ws://127.0.0.1:8998/api/chat, dials wss://127.0.0.1:8999/api/chat upstream. Verified live: moshi_client.available(force=True) -> True, continuous PCM stream -> MoshiAudio event, zero changes to backend/app/services/moshi.py. Found and fixed 3 real PyOgg ctypes bugs + 1 self-introduced framing bug (ogg_stream_flush vs pageout). backend/.venv now runs Python 3.12 (system default 3.14 can't build pydantic-core). Non-blocking follow-up noted by reviewer: relay has no OUR_ERROR translation when the upstream Candle connection itself fails (client sees abrupt close, no diagnostic frame) — worth a fix in M2's production version, not required for M1.
Candle server currently running on port 8999, relay on port 8998 (both left up per implementer's note).

Task 6: complete (commits 0a79927, 9e6d58a — fix commit after 1 review cycle, re-review clean/Approved). ml/moshi/bench_moshi_latency.py measures Moshi time-to-first-audio through the live relay+Candle stack using the real unmodified moshi_client. MEASURED RESULT (M1 headline number for M12): p50=1600.8ms p95=1775.9ms n=10, all attempts=1 (zero retries), warm GPU, q8 weights. This is ~8x the plan's ~200ms target for Moshi alone — attributed mostly to Task 5's throwaway relay overhead (confirmed bugs: 20ms chunks desync the Candle server's 40ms flush quantum, relay segfaults on teardown, relay's Opus encoder crashes on real speech content). Benchmark streams silence not real speech as a documented, reviewed-acceptable deviation. All of this is written up in docs/M1_BRINGUP_LOG.md. M2 should re-run this exact benchmark once a production relay replaces relay.py to isolate real Moshi latency from relay overhead.
Known non-blocking follow-ups for M2 (not required for M1): relay.py's segfault-on-teardown and crash-on-real-speech bugs need real fixes, not workarounds; relay.py has no ERROR-tag translation on upstream connection failure (from Task 5's review).

Task 7: complete (commit a5acd00, review clean/Approved). scripts/make.ps1 and Makefile both got moshi-serve (Candle server only, deliberately not the relay — avoids port conflict with moshi-bench's self-managed relay lifecycle) and moshi-bench targets, verified with real runs (moshi-serve: clean restart confirmed ~23s to ready; moshi-bench: third independent measurement p50=1606.3ms/p95=1760.4ms, consistent with prior runs). docs/M1_BRINGUP_LOG.md reorganized into one coherent document, verified by the reviewer to have lost zero facts from Tasks 1-6 (one stale 8998-port reference was corrected to 8999 in the process).

ALL 7 TASKS OF THE M1 PLAN ARE COMPLETE AND REVIEWED CLEAN.
Final whole-branch review is the next step per subagent-driven-development.
Base commit for whole-branch review: 37abe6dc6e11181a367445a36fb792a9f6dc2301 (repo state before this plan started)
Head: a5acd00

Task 1: complete (commits f79e730..6dd2a00 — 1d9f7a3 implementation + 6dd2a00 fix, review clean/Approved after one fix cycle). OpusOggEncoder in ml/moshi/bridge.py: page_duration=20000 forces per-frame Ogg page flush (25/25 -> confirmed in spike, now backed by test_encoder_emits_bytes_within_one_frame), 40ms/960-sample framing matches Candle's PCM flush quantum, ml/moshi/requirements.txt swapped PyOgg->av==18.1.0. Fix cycle: removed an out-of-scope untested OpusOggDecoder placeholder and a dead AudioResampler import the implementer had added beyond brief scope.

Task 2: FIRST ATTEMPT BLOCKED (no commit, correctly escalated). Implementer built the brief's specified synchronous OpusOggDecoder.feed(chunk)->bytes exactly as written, found it structurally broken: PyAV 18.1.0's Ogg demuxer permanently latches EOF the first time its blocking-simulated reader returns 0 mid-stream, so a design that resumes one container/stream across feed() calls cannot decode past the first EOF. Independently reverified this by the orchestrating session (isolated repro: 1200 bytes decoded, 8700 more valid bytes appended to the same buffer, second demux() call on the same container/stream yields zero packets). Root cause: the design spec's original spike (session, pre-plan) used a BLOCKING reader in a background thread and never hit this path; the plan's Task 2 brief wrote a simpler synchronous feed() that does hit it. User chose fix: redesign OpusOggDecoder around a dedicated worker thread + blocking reader (feed()->None, poll()->bytes), matching what the design spec's architecture section already specified. Verified by a second spike before rewriting the plan; a further spike caught a drain-before-cancel ordering bug in bridge_upstream_to_client's consumption of the new interface (naive immediate cancel on connection close dropped 100% of decoded output in a short real-audio test) and fixed it (await asyncio.to_thread(decoder.close) before cancelling the drain task). Plan corrected and committed: c20235a. Task 3's brief also updated in the same commit since it consumes OpusOggDecoder's interface.

Task 2: complete (commit 0d69f1d, review clean/Approved on retry, no fix cycle needed). OpusOggDecoder in ml/moshi/bridge.py: dedicated worker thread runs av.open()+demux once over a _BlockingReader whose readinto() blocks on queue.Queue.get() rather than ever returning 0 for "nothing yet" -- verified this avoids PyAV 18.1.0's Ogg EOF-latch bug. Public interface: feed(chunk)->None, poll(timeout=0.05)->bytes, close()->None. AudioResampler to 24kHz confirmed correct. Reviewer diffed implementation against the brief byte-for-byte and found it verbatim, no deviations.

Task 3: complete (commits 0d69f1d..fa89059 -- 625301b implementation+first-fix combined, fa89059 second fix, review clean/Approved after two fix cycles). bridge_upstream_to_client/bridge_client_to_upstream/handler/main added to ml/moshi/bridge.py, consuming OpusOggDecoder's feed()/poll() split via a concurrent drain_decoded() task; decoder.close() awaited before drain_task.cancel() per verified spike ordering. Two real bugs found via this task's own TDD process, both fixed and re-reviewed clean:
  1. First bug (found by implementer, correctly escalated BLOCKED): closing a decoder that was never fed any KT_AUDIO (e.g. text-only exchange) raised av.error.EOFError from av.open() on an empty reader, propagating as a spurious OUR_ERROR frame. Fixed in OpusOggDecoder._run() (Task 2-owned code) by catching that EOFError as a benign empty-stream case.
  2. Second bug (found by task review): that fix was too broad -- it also silently swallowed genuine truncation/corruption (some bytes received, probe never completes), which is exactly the failure class Task 3 exists to surface. Fixed by adding a bytes_received counter to _BlockingReader, gating benign-vs-error on whether any bytes were ever actually received, not just exception type. Two new regression tests added (zero-bytes-benign, some-bytes-error) and verified to discriminate correctly.
Full suite: 8/8 passing (3 codec + 2 new decoder-edge-case + 3 protocol).

Task 4: complete (driven directly by orchestrating session per hybrid split, commit 87b3ff7 -- docs only, no code changes this task). Live validation against the running moshi-backend.exe (already up from M1): bridge.py started, real speech fixture (dysfluent_utterance.wav) streamed through 4 consecutive connect/disconnect cycles against the SAME bridge process. All 4 runs succeeded: audio returned every time, one run surfaced Inner Monologue text ("Hello"), zero crashes/segfaults, clean connect/disconnect logging throughout. Direct regression proof for relay.py's segfault-on-teardown bug (which needed a second-or-later connection to manifest) -- confirmed fixed. docs/M1_BRINGUP_LOG.md M2 addendum written.

Task 5: complete (commits 87b3ff7..cfc025f -- 666cc7d implementation + cfc025f doc fix, review clean/Approved after one fix cycle). ml/moshi/bench_moshi_latency.py rewritten to target bridge.py: BridgeHandle starts one process once and reuses it across all N iterations (RelayHandle's per-iteration restart dropped, no longer needed since bridge.py doesn't segfault on teardown); load_fixture_pcm streams the WAV fixture's real decoded speech instead of synthesized silence (no longer needed since bridge.py's encoder doesn't crash on real speech). Makefile/scripts/make.ps1 updated relay.py->bridge.py references. ml/moshi/relay.py and its stray log artifacts deleted. Fix cycle: ml/moshi/serve_moshi.md still pointed developers at the deleted relay.py for manual bring-up -- corrected to bridge.py.
LIVE BENCHMARK RESULTS (run directly by orchestrating session against live GPU/Candle server, per hybrid split -- hands-on hardware work not subagent-dispatched): two independent runs, both with real speech content (backend/scripts/words/dysfluent_utterance.wav) and zero retries needed (bridge stability confirmed). Run 1 (accidentally against a stray leftover bridge process from Task 4, not the bench script's own subprocess): p50=1952.5ms p95=4181.6ms n=10. Run 2 (bench script managing its own bridge subprocess end-to-end, the proper Step 2 execution): p50=2009.9ms p95=2645.0ms n=10. Both consistent in magnitude. This is the first M12-relevant measurement with REAL speech content, vs M1's p50=1600.8ms/p95=1775.9ms measured with silence through the old relay -- real content appears to add meaningful latency and variance. Worth flagging for the M12 thesis narrative writeup: the ~2000ms real-speech number, not the ~1600ms silence number, is the honest Moshi-side latency figure.

Task 6: complete (commit 347768c, review clean/Approved, no fix cycle needed). docs/PROJECT_PLAN.md M2 row marked DONE (2026-09-05) with p50=2009.9ms/p95=2645.0ms (Task 5 Run 2's numbers), matching M1 row's format exactly.

ALL 6 TASKS OF THE M2 PLAN ARE COMPLETE AND REVIEWED CLEAN.
Final whole-branch review is the next step per subagent-driven-development.
Base commit for whole-branch review: f79e730 (plan committed, repo state before this plan's implementation started)
Head: 347768c

FINAL WHOLE-BRANCH REVIEW: three-round fix cycle before merge-ready.
Round 1 (base dd17613, head 347768c): found one Important cross-task interaction bug the per-task reviews couldn't see -- handler()'s normal client-disconnect path cancels bridge_upstream_to_client, and if that cancellation lands while the decoder's teardown queues a real decode error, the outer except Exception caught that error instead of letting CancelledError propagate, sending a spurious OUR_ERROR frame on ordinary disconnect (which the frozen client contract escalates to a full session degrade to cascade). Also found stale .gitignore entries.

Fix attempt 1 (b4b2770): wrapped decoder.close() in suppress(Exception) -- WRONG, close() never raises decode errors, fixed nothing. Its regression test was also flaky (13-25/20-25 failure rate on both buggy and "fixed" code).

Re-review 1: caught fix attempt 1 was wrong via independent reproduction (11-18/30 spurious errors still occurring) and caught the test's flakiness via repeated runs. Correctly identified the real leak: `await drain_task` only suppressed CancelledError, not other exceptions poll() could raise instead.

Fix attempt 2 (a3aef6c): moved suppression to `with contextlib.suppress(BaseException): await drain_task` -- fixed the cancellation bug (verified 75/75 clean) but introduced a NEW regression: unconditional suppression also swallowed real decode errors on the completely normal, non-cancelled path (measured 8/10 -> 0/10 for a corrupt stream ending without cancellation), undoing one of M2's three core goals (surfacing real failures instead of hiding them, per the module's own docstring).

Re-review 2: caught the normal-path regression via independent measurement (the existing suite didn't catch it -- no test exercised "real decode error, no cancellation involved"). Suggested the correct shape: use sys.exc_info() to distinguish "cancellation in flight" (discard drain_task's exception) from "normal completion" (re-raise it so the client still gets OUR_ERROR).

Fix attempt 3 (a13aa8b): implemented the sys.exc_info()-based distinction correctly. Added a new deterministic test for the normal-path-error case (using a threading.Event-controlled OpusOggDecoder double to avoid real-thread timing races, itself verified 100/100 in isolation before use) alongside the existing cancellation test.

Re-review 3 (FINAL, clean): independently verified BOTH requirements hold simultaneously -- 35/35 on the cancellation test, 35/35 on the new normal-path-error test, 11/11 full suite, PLUS an end-to-end sanity check with the REAL (non-mocked) OpusOggDecoder confirming OUR_ERROR reaches the client 9-10/10 at adequate settle time, and an explicit A/B against attempt 2's broken logic (0/10) proving the fix is load-bearing, not passing by coincidence. Also independently confirmed the sys.exc_info()-in-async-finally mechanism actually behaves as intended on this Python version (3.12.10) rather than assuming it. One residual minor note: a pre-existing (not introduced or worsened by this fix) timing race in asyncio.to_thread+cancel() semantics can occasionally lose an error frame if cancellation lands mid-poll() with very little settle time -- documented as non-blocking, matches the pre-M2 baseline behavior in kind.

READY TO MERGE: Yes, per final whole-branch review.
Final head commit: a13aa8b
